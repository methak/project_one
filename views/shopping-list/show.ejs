<!DOCTYPE html>
<html>
    <head>
      <meta charset="utf-8">
      <link rel="stylesheet" type="text/css" href="/css/style.css" />
      <title>Shopping List Show Page</title>
    </head>
    <body class=”font-body”>
      <div class="flex justify-center items-center">
        <div class="container flex rounded-lg my-4 p-4 mx-auto h-full bg-indigo-50 justify-center">
          <div class="flex gap-x-4 md:w-2/3 lg:w-1/3 xl:max-w-full">
            <div class="w-full bg-purple-200">
              <div class="my-4">

                <h1 class="text-center mb-3">Shopping List Show Page</h1>
                <div class="mx-10">
                  Store Name: <%=shoppingList.storeName%><br>
                  Store Address: <%=shoppingList.storeAddress%><br>
                  Store Hours: <%=shoppingList.storeHours%><br>
                  Total Price: <%=shoppingList.totalPrice%><br>
                  Date: <%= shoppingList.updatedAt%>
                </div>
              </div>
              <div class="flex justify-between">
                <button class=" bg-purple-500 hover:bg-purple-700 text-white font-bold py-2 px-4 rounded-full">
                  <a href="/shopping-list">Back</a>
                </button>

                <button class=" bg-purple-500 hover:bg-purple-700 text-white font-bold py-2 px-4 rounded-full">
                  <a href="/shopping-list/<%=shoppingList._id%>/edit">Edit</a>
                </button>

                <form method="POST" action="/shopping-list/<%=shoppingList._id%>?_method=DELETE">
                  <button class=" bg-purple-500 hover:bg-purple-700 text-white font-bold py-2 px-4 rounded-full" type="submit">Delete</button>
                </form>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
